{
  "name": "dash-for-cloudflare",
  "version": "1.0.0",
  "private": true,
  "description": "Flutter web app with Eleventy wrapper for Cloudflare Pages cache",
  "scripts": {
    "build": "chmod +x web-build.sh && ./web-build.sh"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
